require "spec_helper"

describe "pelt configures an install with explicit source paths" do
  
  it "supports relative paths" do
    fail "need to show the implicit or expllicit configuration considers relative spec"
   
 end
 
end
 
 